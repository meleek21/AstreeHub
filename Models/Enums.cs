namespace ASTREE_PFE.Models
{
    public enum RoleType
    {
        SUPER_ADMIN,
        DIRECTOR,
        EMPLOYEE
    }

    public enum UserStatus
    {
        Active,
        AVAILABLE,
        BUSY,
        OFFLINE
    }
}
